import { NavcontrolDispalyDirective } from './navcontrol-dispaly.directive';

describe('NavcontrolDispalyDirective', () => {
  it('should create an instance', () => {
    const directive = new NavcontrolDispalyDirective();
    expect(directive).toBeTruthy();
  });
});
